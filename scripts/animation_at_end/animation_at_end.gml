/// @description animation_at_end()
/*
    Will return true at the last frame of an animation.
    You must have a positive animation speed.
*/

var result = animation_hit_frame(image_number-1);
return result;
