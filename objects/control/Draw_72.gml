/// @description Set draw target to outline surface

/*if (!surface_exists(outline_surface)) {
	outline_surface = surface_create(surface_get_width(application_surface), surface_get_height(application_surface));
}

surface_set_target(outline_surface);
draw_clear(c_white);*/